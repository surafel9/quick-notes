// Content script for the extension (execute code on web pages)
// This script can interact with the actual web page

// Example: Display a message in the console when the extension is loaded on a page
console.log('QuickNote content script loaded.');
